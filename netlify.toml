[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA routing — send all non-file paths to index.html
# Netlify automatically handles /.netlify/functions/* before redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
